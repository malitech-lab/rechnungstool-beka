# Rechnungstool Montageservice Beka – Laufzeit-Abhängigkeiten
# Installation:  pip install -r requirements.txt
Flask==3.1.3
fpdf2==2.8.7
lxml==6.1.1
drafthorse==2025.2.0
segno==1.6.6
Pillow==12.2.0
# Pillow wird von fpdf2 benötigt, um Logo und QR-Code in das PDF einzubetten.
pypdfium2==5.10.1
# pypdfium2 (PDFium, BSD-Lizenz) rendert die PDF-Vorschau als Bild – funktioniert
# zuverlässig in jedem Browser. KEIN AGPL wie PyMuPDF.
pystray==0.19.5
# pystray: Tray-Icon zum sauberen Beenden, wenn die EXE ohne Konsolenfenster
# (console=False) läuft. Nutzt Pillow (bereits vorhanden). Nur Windows-relevant.
certifi==2026.6.17
truststore==0.10.4
# TLS-Vertrauensspeicher für den E-Mail-Versand (SMTP), gegen CERTIFICATE_VERIFY_FAILED:
# truststore nutzt den Betriebssystem-Speicher (Windows findet so auch fehlende
# Zwischenzertifikate) und ist die robusteste Variante; certifi ist der Fallback.
# Hinweis: Das Logo liegt fertig als PNG/SVG bei (app/static/img/). cairosvg wird
# NUR zum Neu-Erzeugen des Logos benötigt und ist KEINE Laufzeit-Abhängigkeit.
# Werkzeug/Jinja2 werden von Flask mitinstalliert.
# Nur für den Windows-Build (optional):
# pyinstaller==6.11.1
